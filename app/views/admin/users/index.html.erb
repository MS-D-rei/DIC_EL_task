
<table>
  <thead>
    <tr>
      <th><%= sort_order 'name', 'Name' %></th>
      <th><%= sort_order 'tasks_count', 'Task count' %></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.tasks_count %></td>
      <td><%= link_to t('view.details'), admin_user_path(user), class: 'btn btn-primary' %></td>
      <td><%= link_to t('view.edit'), edit_admin_user_path(user), class: 'btn btn-secondary' %></td>
      <td><%= link_to t('view.destroy'), admin_user_path(user), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate(@users) %>